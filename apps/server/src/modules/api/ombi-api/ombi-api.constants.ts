// Holds the run-scoped request index (one movie and one show list per rule
// run). Flushed between rule-group runs like the Seerr one.
export const OMBI_REQUESTS_CACHE_ID = 'ombirequests';
export const OMBI_REQUESTS_CACHE_KEY = 'request-index';
